diff --git a/drivers/xgl/icd/api/resource_tracking/CmdBufferData.h b/drivers/xgl/icd/api/resource_tracking/CmdBufferData.h
index a20a65ffc3..cd0c871cbc 100644
--- a/drivers/xgl/icd/api/resource_tracking/CmdBufferData.h
+++ b/drivers/xgl/icd/api/resource_tracking/CmdBufferData.h
@@ -29,6 +29,9 @@ public:
         ResourceHandle             handle;
         ResourceType               type;
         SubresourceRangeAccessData accessData;
+        // Cached at add-time, when the underlying VkImage is known-live -- lets later passes (which may run
+        // after the resource has otherwise gone stale) consult this without re-dereferencing the raw handle.
+        bool                       hasMisalignedMetadataWrite = false;
     };
 
     // Stores captured decriptor metadata from pipeline stages
diff --git a/drivers/xgl/icd/api/resource_tracking/ResourceTrackerBuilder.cpp b/drivers/xgl/icd/api/resource_tracking/ResourceTrackerBuilder.cpp
index 73a5255f38..63753ca21d 100644
--- a/drivers/xgl/icd/api/resource_tracking/ResourceTrackerBuilder.cpp
+++ b/drivers/xgl/icd/api/resource_tracking/ResourceTrackerBuilder.cpp
@@ -1697,7 +1697,8 @@ void ResourceTrackerBuilder::ProcessBoundResourcesForCompute(NodeIndex computeNo
                             addResource);
 
         // Add the resource, both to the tracker, and the dirty resource list.
-        bool        is3dImage = false;
+        bool        is3dImage                        = false;
+        bool        newlyAddedStorageImageMisaligned = false;
         ResourceRef resource;
         if (addResource)
         {
@@ -1718,6 +1719,17 @@ void ResourceTrackerBuilder::ProcessBoundResourcesForCompute(NodeIndex computeNo
                                                       imageSampleCount);
 
                 is3dImage = (imageType == static_cast<uint32_t>(VK_IMAGE_TYPE_3D));
+
+                // pImage is known-live right here -- this is the only point where it's safe to query it. Cache
+                // the result on the dirty-resource entry below instead of re-deriving it from the raw handle on
+                // every later write observation (that re-derivation is what used to use-after-free once this
+                // resource's backing VkImage had been destroyed by a later, unrelated command buffer/frame).
+                if ((computeResource.descTypeBits == DescriptorTypeBits::DescriptorTypeStorageImage) &&
+                    ((accessType == ResourceAccessType::Write) || (accessType == ResourceAccessType::ReadWrite)) &&
+                    pImage->PalImage(DefaultDeviceIndex)->HasMisalignedMetadata())
+                {
+                    newlyAddedStorageImageMisaligned = true;
+                }
             }
             else if (computeResource.descTypeBits == DescriptorTypeBits::DescriptorTypeStorageBuffer)
             {
@@ -1730,6 +1742,16 @@ void ResourceTrackerBuilder::ProcessBoundResourcesForCompute(NodeIndex computeNo
                                              Utilities::ConvertDescriptorTypeToResourceType(
                                                  computeResource.descTypeBits),
                                              SubresourceRangeAccessData(subresRange, accessType));
+
+            if (newlyAddedStorageImageMisaligned)
+            {
+                // Immediately after our own AddDirtyResource call, before any other Add* call can invalidate the
+                // pointer (see FindDirtyResource's contract in CmdBufferData.h).
+                if (auto* pJustAdded = m_cmdBufferData.FindDirtyResource(handle))
+                {
+                    pJustAdded->hasMisalignedMetadataWrite = true;
+                }
+            }
         }
 
         // Add the resource as an input/output to the compute node.
@@ -1744,10 +1766,15 @@ void ResourceTrackerBuilder::ProcessBoundResourcesForCompute(NodeIndex computeNo
             if ((computeResource.descTypeBits == DescriptorTypeBits::DescriptorTypeStorageImage) &&
                 ((accessType == ResourceAccessType::Write) || (accessType == ResourceAccessType::ReadWrite)))
             {
-                auto* pImage = vk::Image::ObjectFromHandle(reinterpret_cast<VkImage>(handle));
-                if ((pImage != nullptr) && pImage->PalImage(DefaultDeviceIndex)->HasMisalignedMetadata())
+                // Read the cached bit rather than re-deriving it from the raw handle: on the foundResource path,
+                // this resource may have been added long before this call (a prior dispatch/subpass), and its
+                // backing VkImage is not guaranteed to still be live -- see the comment at the add site above.
+                if (auto* pDirty = m_cmdBufferData.FindDirtyResource(handle))
                 {
-                    m_hasPendingMisalignedMetadataWrite = true;
+                    if (pDirty->hasMisalignedMetadataWrite)
+                    {
+                        m_hasPendingMisalignedMetadataWrite = true;
+                    }
                 }
             }
 
