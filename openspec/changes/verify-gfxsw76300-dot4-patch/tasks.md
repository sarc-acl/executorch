## 1. Build the patched driver

- [x] 1.1 In the SUMD checkout, checkout the first-bad commit `69e887275e26ae4a44e2d6e14bd3e600cec67ac8` — **failed the plan-B path**: the patch's file layout (`bilInstructionVectorMatrix.cpp` under `instruction/`, `scpc_bil_utils.cpp` at all) doesn't exist yet at this commit, so per design.md fell back to SUMD `main` tip (`b015e79a56`) instead. Built two worktrees there: `dot4patch-maintip-baseline` (unpatched control) and `dot4patch-maintip-patched`.
- [x] 1.2 Apply Jayati's patch (`Id11af22dd1ddc549339a90074ea22d61375da7b8`, "sc: Enable dot4 instructions") on top — `git am` of the literal JIRA attachment failed on `scpc_bil_utils.cpp` (line-number drift/reflow only, not a real conflict); manually re-applied the identical 3-file logic by hand in `dot4patch-maintip-patched`, diffed against the attachment to confirm equivalence, committed as `75616cb859` on branch `yanwen/gfxsw76300-dot4-patch-maintip`. **Base-commit change: main tip (`b015e79a56`, 2026-07-20), not `69e887275e`** — comparison against the last-good/first-bad bisect baselines is no longer apples-to-apples; task 1.3b adds an unpatched-main-tip baseline run to compensate.
- [x] 1.3 Build SUMD (`dot4patch-maintip-patched`) and record the resulting driver's on-device md5 — first build (without UAF fix) succeeded, md5 confirmed on-device during 2.1; superseded by the rebuild in 2.7 below
- [x] 1.3b Also build the unpatched `dot4patch-maintip-baseline` worktree, so we have a same-commit A/B (patched vs unpatched main tip) in addition to the original last-good/first-bad bisect numbers — same superseded-by-2.7 note applies

## 2. Verify on M41 (bisect device)

- [x] 2.1 `ssh xgpusw-debug07`, `export ANDROID_SERIAL=00000bb7cc34abd3`, flash the patched build, confirm it boots and the app launches without crashing — staged binary+PTEs+tokenizer+prompt to `/data/local/tmp/llama_vk` (nothing was there before, different device from the original bisect's `00000a34cdd4abd3`); driver flashed and identified fine (md5 `46ebe0d91913a54efed9d3cf3e6ff649` for the unpatched build)
- [x] 2.2 Run `scripts/bisect-test.sh` (or the equivalent manual steps: Llama 3.2 1B, 4w and 8da4w, 2048-token prefill, pinned 980/5333/800, `llama_main_rel1.3`) and record prefill/decode tok/s for both quant modes — via the forked `verify-test.sh`, both with the UAF fix applied: **unpatched main-tip control**: 4w=607.4, 8da4w=428.6 tok/s, verdict=bad (regression still present, as expected). **Dot4-patched main-tip**: 4w=606.5, 8da4w=810.8 tok/s, verdict=good — 4w unchanged, 8da4w recovered to 99.9% of the original last-good baseline (811.4)
- [x] 2.3 Pull and inspect shader disassembly for the 8da4w matmul kernel; count `v_dot4_i32_i8`/`v_dot4_u32_u8` occurrences and compare against last-good (128) and first-bad (0) — used the on-device pipeline-dump property (`debug.mariner.Feature::LegacySettings::EnablePipelineDump`) per `.shared-context/instruction-for-ai/result-and-report/README.md` §3c. Same pipeline hash `PipelineCs_0xE1626A9FA3C753BB` (same SPIR-V input) on both drivers: unpatched = 0 dot4 / 70 emulated-fallback ops, patched = **128 dot4** / 2 fallback ops. 128 exactly matches the historical last-good count; 0 exactly matches first-bad. Saved to `results/isa-dump/`.
- [x] 2.4 Spot-check decoded output text for both quant modes is coherent (no garbage-token pattern) — both runs produced coherent English text (the same repeating-prompt-echo pattern, expected with `--max_new_tokens=1 --warmup=true`), no garbage tokens on either driver
- [ ] 2.5 If a CTS harness for `integer_dot_product` is available on this device/build, run it and record pass/fail — no CTS harness found on this device/workstation; skipping, relying on Jayati's own manual CTS pass report plus our disassembly+throughput evidence
- [x] 2.6 If the build crashes or hangs, re-flash the last-good driver (`b460447da2...`) immediately and record what happened before continuing — **hit this on the very first run**, on the *unpatched* main-tip baseline's plain 4w warmup (before the patch was even in play). SIGSEGV, tombstone shows `SEGV_MAPERR` inside `vulkan.samsung.so` called from `llama_main_rel1.3`'s Vulkan graph exec. Did not need to re-flash last-good since the device stayed responsive for the next adb command; proceeded straight to 2.7 instead of a full recovery re-flash
- [x] 2.7 If the crash matches the GFXSW-76434 UAF signature, fetch and check out the fix (`git fetch https://yanwen.xu@gerrit.sarc.samsung.com/a/sumd refs/changes/92/65292/9`), re-apply Jayati's dot4 patch on top, rebuild, and retry 2.1-2.5 — confirmed match (fetched patch set 9 = commit `f1321660289ea9476d2389aaafa0cad9939759a7`, "xgl: fix stale descriptor type on reused binding", parent is exactly main tip `b015e79a56`, so a clean single-commit cherry-pick). Applied to **both** worktrees, since the crash hit the unpatched baseline too: `dot4patch-maintip-baseline` → `46e41723d8`, `dot4patch-maintip-patched` → `cb993c15f1` (on top of the dot4 patch, `75616cb859`). Both rebuilding now; will retry 2.1-2.5 once done.

## 3. Verify on M51 (primary board, only if M41 passed cleanly)

- [x] 3.1 `ssh yanwen.xu@sj1-dmckee-d01`, flash the patched build to the primary M51 board, re-verifying the driver hash per `.shared-context/ACTIVE-STATUS.md` convention — staged missing `llama3_2_1b_4w_texture_ctx3072.pte` (everything else was already on-device from prior sessions), flashed `dot4patch-maintip-patched`, confirmed md5 `cfe0caf0f41322088fce75c22a2288ec` on-device, pinned to M51's standard 509/2730/663 (verified via devfreq readback, not just the pin script's own echo)
- [x] 3.2 Run the same 4w vs 8da4w 2048-token prefill comparison at M51's own pinned clocks — **finding: the dot4 patch crashes M51 on 8da4w; the unpatched baseline does not.** 4w completed cleanly on the patched build (310.6 tok/s). First 8da4w attempt on the patched build: board dropped off `adb` AND `fastboot` entirely mid-run (no tombstone). Recovered it once it resurfaced in fastboot (`fastboot reboot`; driver survived, md5 re-verified, re-pinned 509/2730/663), retried the identical 8da4w run on the same patched build — **crashed the exact same way again** (2/2). To isolate cause, flashed the **unpatched** `dot4patch-maintip-baseline` (base+UAF-fix, no dot4 patch) and ran the identical 8da4w command: completed cleanly, no crash, device stayed connected throughout, 222.6 tok/s (consistent with the regression still being present, as expected without the patch). Pipeline-dump property was never enabled on M51 in this session, ruling out the one other known crash-prone mechanism on this board (per `results/isa-dump/m5-evt1/README.md`'s crash caveat) as the cause. **Conclusion: this is not board flakiness — the dot4 patch itself triggers a hard crash on M51 (Xclipse 970) under 8da4w that does not occur on M41.** Board left rebooted back to Android on the unpatched driver, in a known-good state, after this test.
- [x] 3.3 Spot-check output coherence for both quant modes — 4w (patched) and 8da4w (unpatched only) both produced coherent output; 8da4w never completed on the patched build due to the crash in 3.2

## 4. Report results

- [x] 4.1 Compile a results table (throughput + dot4 instruction count, M41 and M51 if run) against the last-good/first-bad baselines from the original bisect — see `results/isa-dump/README.md` for M41's ISA table; M51 has no successful patched-8da4w data point to include (see 3.2)
- [ ] 4.2 Post the results as a comment on GFXSW-76300 for Jayati and Pavan, stating: patch fully fixes the regression on M41 (throughput + disassembly confirmed), but **reproducibly crashes M51 (2/2) under 8da4w** — needs Jayati's investigation before this patch can be considered safe to land, not just effective
